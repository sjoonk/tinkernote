<div id="new_page" class="float right">
	<%= button_to '새 페이지', new_page_path %>
</div>	

<%= page_title APP_CONFIG[:home_title], :h3 %>

<div id="summary_area" class="clearfix">
	<div class="right">
		총 <%= @pages_count %>개의 페이지
	</div>	
</div>	

<table>
<% @pages.each do |page| %>
<tr>
	<td><%= link_to page.title, page_path(page) %></td>
	<td><%= truncate(strip_tags_and_wiki(page.body), 20) %></td>
	<td><%= human_time(page.updated_at) %></td>
	<td><%= link_to page.user %></td>
	<td><%= link_to image_tag('delete.gif'), page_path(page), :method => :delete, :confirm => 'Sure?' %>
</tr>
<% end %>
</table>

<div class="apple_pagination">
	<!--div clas="page_info">
		<%#= page_entries_info @pages %>
	</div-->
	<%= will_paginate @pages, :container => false %>
</div>

<% content_for :west_area do %>
날짜별
<% end %>
<% content_for :east_area do %>
태그?
<% end %>	